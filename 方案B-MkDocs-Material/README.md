# MkDocs Material 运行说明

## 安装

```powershell
python -m pip install mkdocs-material
```

## 本地预览

```powershell
python -m mkdocs serve
```

然后打开终端输出的本地地址，通常是 `http://127.0.0.1:8000/`。

## 构建静态站

```powershell
python -m mkdocs build
```

输出目录为 `site/`。

## 适合用途

更适合作为 VCP 项目文档中心和内部知识库，是我更推荐长期维护的方案。